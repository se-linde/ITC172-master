@model IEnumerable<FinalProjV2.Models.Donation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DonationDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DonationAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DonationConfirmation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person.PersonLastName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DonationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonationAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonationConfirmation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.PersonLastName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DonationKey }) |
            @Html.ActionLink("Details", "Details", new { id=item.DonationKey }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DonationKey })
        </td>
    </tr>
}

</table>
